/*function deletePhoto(photoId) {
    fetch(`http://localhost:5678/api/works1/ + id`, {
      method: 'DELETE',
      headers: {
        'accept': '*//**',
        Authorization: `Bearer ${token}`,
      }
    })
      .then(function(response) {
        if (response.status === 204) {
          console.log('Photo supprimée avec succés.');
        } else {
          console.error('Echec de la suppression de la photo.');
        }
      })
      .catch(function(error) {
        console.error('Une erreur s\'est produite lors de la suppression de la photo :', error);
      });
  }

  modalCloseButton.addEventListener('click', closeModal);

  window.addEventListener('click', function(event) {
    if (event.target === modal) {
      closeModal();
    }
  })

  openModal();

});*/
